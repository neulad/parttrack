# Copy to .env and fill in values
JWT_SECRET=change-me-to-a-long-random-string
NODE_ENV=development
APP_URL=http://localhost

# Email — leave blank in dev to skip sending (logged to console instead)
EMAIL_HOST=
EMAIL_PORT=587
EMAIL_USER=
EMAIL_PASS=
EMAIL_FROM=noreply@parttrack.dev
